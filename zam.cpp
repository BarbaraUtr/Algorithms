#include <bits/stdc++.h>

using namespace std;

#define LOL 100005
#define XD 1003

int pl[XD][XD];
int tab [LOL][4];
int ri[XD][XD];
int dow[XD][XD]

int main ()
{
	int n, m, q;
	
	scanf("%d%d%d", &m, &n, &q);
	
	for(int i=0; i<n; ++i)
	{
		for(int k=0; k<4; ++k)
		{
			scanf("%d", &tab[i][k]);
		}
	}
	
	for(int)
}
